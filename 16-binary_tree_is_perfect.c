#include "binary_trees.h"

/**
 * binar_tree_is_perfect - checks if binary tree is perfect
 * @tree: tree pointer
 *
 * Return: tree on success, 0 if tree is NULL
 */

int binary_tree_is_perfect(const binary_tree_t *tree)
{
	
}
